import { Typography } from "@mui/material";

const QueList = () => {
	return (
		<div
			style={{
				margin: "1rem 0",
			}}
		>
			<Typography variant="button" color="pink">
				QUEUE (5)
			</Typography>
		</div>
	);
};

export default QueList;
